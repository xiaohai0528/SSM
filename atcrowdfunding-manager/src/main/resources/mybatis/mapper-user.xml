<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.atcrowdfunding.dao.UserDao" >

    <select id="pageQueryData" resultType="com.atguigu.atcrowdfunding.bean.User">
        select * from t_user limit #{start}, #{size}
    </select>

    <select id="pageQueryCount" resultType="int">
        select
            count(*)
        from t_user
    </select>

    <!--<select id="query4LoginEmail" resultType="int">
        select count(*) from t_user where loginacct = #{loginacct} or  email = #{email}
    </select>-->

    <select id="queryLoginEmail" resultType="com.atguigu.atcrowdfunding.bean.User">
        select * from t_user where loginacct = #{loginacct} or  email = #{email}
    </select>


</mapper>